#include<stdio.h>
#include<conio.h>
void main(void)
{
    int y,s;
    scanf("%d",&y);
        scanf("%d",&s);

        if(y>s)
        {
            printf("you r older");
        }

        else
        {
            printf("sister older");
        }
}