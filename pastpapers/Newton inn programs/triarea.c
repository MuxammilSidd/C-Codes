#include<stdio.h>
#include<conio.h>
void main(void)
{
    float b,h,a;
    printf("ENter base: ");
    scanf("%f",&b);
    printf("enter h: ");
    scanf("%f",&h);
    a= (0.5)*b*h;
    printf("area is %f",a);
   
}